using System.Net.Sockets;
using System.Windows.Forms;

namespace lr8
{
    public partial class Form1 : Form
    {
       /* public Form1()
      //  Bitmap bitmap;
      //  Pen pen;
       // Rectagle re;
       // Ellipse el;
       // Square sq;
        //Circle c;
        {
            InitializeComponent();
        }

        private void label7_Click(object sender, EventArgs e)
        {

        }

        private void button5_Click(object sender, EventArgs e)
        {
            try
            {
                re = new Rectagle(int.Parse(textBox1.Text), int.Parse(textBox2.Text), int.Parse(textBox3.Text), int.Parse(textBox4.Text));
                if (int.Parse(textBox1.Text) + int.Parse(textBox3.Text) <= Init.pictureBox.Width && int.Parse(textBox2.Text) + int.Parse(textBox4.Text) <= Init.pictureBox.Height)
                {
                    re.Draw();
                    ShapeContainer.AddFigure(re);
                    listBox1.Items.Add(re);
                }
                else MessageBox.Show("Введите другие значения. Рисунок вышел за границу");
            }
            catch
            {
                MessageBox.Show("Неверное введены данные! Введите данные повторно");
            }
        }
       */
    }
}